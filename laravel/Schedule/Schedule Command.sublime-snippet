<snippet>
	<content><![CDATA[
\$schedule->command(${1:EmailsCommand}::class, ['${2:--force}'])
	->withoutOverlapping()->timezone('Asia/Shanghai')->cron("* * * * *");
	//->before(function() {})->after(function() {})
	//->pingBefore(\$url)->thenPing(\$url)
	//->sendOutputTo($filePath)->appendOutputTo($filePath)->emailOutputTo('foo@example.com');
]]></content>
	<tabTrigger>schedule_command</tabTrigger>
	<scope>source.php</scope>
	<description>Schedule Command</description>
</snippet>
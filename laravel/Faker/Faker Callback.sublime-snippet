<snippet>
    <content><![CDATA[
function () use(\$faker) {
	return factory(User::class)->create()->id;
    return \$faker->randomElement(User::all()->pluck('id')->toArray());
},
]]></content>
    <tabTrigger>fk_callback</tabTrigger>
    <scope>source.php</scope>
</snippet>

<snippet>
    <content><![CDATA[
if(! auth()->attempt(request(['email', 'password']), ${1:remember})) {
    return back();
    return back()->withErrors(['message'=>'username or password not right']);
}
]]></content>
     <tabTrigger>auth_attempt</tabTrigger>
     <scope>source.php</scope>
</snippet>

<snippet>
	<content><![CDATA[
Auth::provider('${1:riak}', function (\$app, array \$config) {
	// place in AuthServiceProvider@boot
    // Return an instance of Illuminate\Contracts\Auth\UserProvider
    return new RiakUserProvider(\$app, \$config);
});
]]></content>
	<tabTrigger>auth_custom_provider</tabTrigger>
	<scope>source.php</scope>
	<description> ⌘ Laravel Auth Custom Provider </description>
</snippet>